node_modules
*.db
.env
.DS_Store

# local preview helper (Claude preview MCP) — machine-specific paths
.claude/launch.json

# private admin docs + any local admin secret (never publish)
ADMIN.md
*.admin-secret
